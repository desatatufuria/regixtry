name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run Go test suite
        run: go test ./...

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v7
        with:
          distribution: goreleaser
          version: "~> v2"
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify release artifacts
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob

          amd64=(dist/regixtry_*_linux_amd64.tar.gz)
          arm64=(dist/regixtry_*_linux_arm64.tar.gz)
          checksums=(dist/regixtry_*_checksums.txt)

          if [ "${#amd64[@]}" -ne 1 ]; then
            echo "expected exactly one amd64 archive, found ${#amd64[@]}"
            exit 1
          fi

          if [ "${#arm64[@]}" -ne 1 ]; then
            echo "expected exactly one arm64 archive, found ${#arm64[@]}"
            exit 1
          fi

          if [ "${#checksums[@]}" -ne 1 ]; then
            echo "expected exactly one checksum file, found ${#checksums[@]}"
            exit 1
          fi

          grep -F "$(basename "${amd64[0]}")" "${checksums[0]}" >/dev/null
          grep -F "$(basename "${arm64[0]}")" "${checksums[0]}" >/dev/null

      - name: Run installer smoke verification
        shell: bash
        run: RUN_INTERACTIVE_CHOOSER=0 bash docs/verification/scripts/install-release-smoke.sh --release-dist dist

      - name: Run container smoke verification
        shell: bash
        run: bash docs/verification/scripts/container-release-smoke.sh --image "ghcr.io/desatatufuria/regixtry:${GITHUB_REF_NAME}" --expect-multiarch --auth-postgres
